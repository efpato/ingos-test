ingos-tests
================

Selenium tests for www.ingos.ru

# Requirements

 - Install Firefox
 - Install requirements:

```bash
$ pip install -r requirements
```

# Usage

To start tests:

```bash
$ nosetests -v tests.py
```
